# Создаем список для заголовков
titles = []
# Цикл для ввода заголовков
while True:
    title = input("Введите заголовок заметки (или нажмите Enter для завершения): ")
    if title == "":
        break #Завершение ввода, если пользователь нажал Enter
    titles.append(title)# Добавление заголовка в список
# Вывод всех заголовков
print("Список заголовков:")
for title in titles:
    print(title)
# Запрашиваем статуса заметки
status = input("Введите статус заметки: ")
# Вывод статуса заметки
print("Статус заметки:", status)
